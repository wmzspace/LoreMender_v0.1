# 2026-05-24 工作记录

> 会话主线:LoreMender 迁移后的发布收尾——README/LICENSE 恢复、CLAUDE.md 忽略、master→main 重命名、首次 GitHub 推送,以及建立 `agent/context/` 日记习惯。

## 一、本次会话产物

### 提交(均已推送 origin/main)
| commit | 内容 |
|---|---|
| `b6177ae` | 迁移至 Vite + React + TypeScript(详见 [0523.md](./0523.md)) |
| `2e51fff` | docs(agent): 添加 0523 会话工作记录 |
| `<本提交>` | docs(agent): 添加 0524 会话工作记录 |

### 仓库状态变化
- 远端 `git@github.com:wmzspace/LoreMender_v0.1.git`:从空仓 → 主分支 `main`,3 个 commit
- 根目录恢复 `README.md`(中文项目介绍)+ `LICENSE`(MIT),v0 原型保留在 `original/`
- `.gitignore` 末尾增加 claude-mem 元数据屏蔽规则

### 新增文件
- `agent/context/0523.md`:Vite 迁移工作记录
- `agent/context/0524.md`:本文件

## 二、本日关键决定(用户 AskUserQuestion 回应)

| 选项 | 决定 |
|---|---|
| Vite 默认 README 覆盖了中文 README | **恢复**原中文 README,再更新其中技术细节为新结构 |
| 根目录 LICENSE 被删 | **复制回根**(公开仓必备) |
| CLAUDE.md(claude-mem 自动产物) | **加入 .gitignore 不提交**,规则:`CLAUDE.md` + `**/CLAUDE.md` |
| 本地分支 master vs 远端 main | **重命名为 main 再推送**(GitHub 默认习惯) |

## 三、用户偏好确认

1. **日记式上下文文档**:用户主动要求在 `agent/context/<MMDD>.md` 留会话记录,推测是跨会话接续的固定工作流——下次会话开头优先读取最近的 `agent/context/*.md`
2. **提交 + 推送的指令很简短**:用户说"提交并推送"或"提交并推送到 <url>"时,期望:
   - 自动判断要 add 哪些文件(忽略 CLAUDE.md 这类元数据)
   - 自动写中文 commit message + Claude 协作者签名
   - push 前如有歧义决策,用 AskUserQuestion 一次性问完(不要单条单问)
3. **不要先建子目录再让用户上移**:像 v0→Vite 这种迁移,直接在仓库根做改造更顺手(0523 session 时建了 `loremender/` 子目录,用户手动上移了)

## 四、远端可见性

- 主页:https://github.com/wmzspace/LoreMender_v0.1
- README 渲染入口:用户原中文 README(含 emoji 标题、徽章、扩展指南、贡献指南、路线图)已更新文件结构与启动命令
- 公开许可:MIT(根 LICENSE)

## 五、待续项(从 0523.md 路线图继承)

未完成,等用户优先级:
- [ ] 第二个副本(李白·谪仙遗恨 / 岳飞·风波未平)
- [ ] AI 生成美术资源替换 SVG 占位
- [ ] 音效 + BGM、动画细节、存档系统、成就系统
- [ ] 国际化

工程侧可立即做(无需用户决策):
- 配 `npm run lint`(ESLint type-aware 规则)
- 抽取 `Topbar` 的 back/menu 重复 SVG 为 `IconBack` / `IconMenu`(StoryPage、CluePage、TrustRoutePage、ProgressPage、GalleryPage 各 1 处重复内联)
- 给 `EndingPage` 的 `useEffect(() => {...}, [])` 改为正确依赖数组(目前用 eslint-disable 抑制了)
